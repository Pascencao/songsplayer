import { RemoveNewLinePipe } from './remove-new-line.pipe';

describe('RemoveNewLinePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveNewLinePipe();
    expect(pipe).toBeTruthy();
  });
});
